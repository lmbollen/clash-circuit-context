dist-newstyle/
.ghc.environment.*
cabal.project.local*
/manual-instrumentation.vcd
/auto-instrumentation.vcd
/notation.vcd
*.hi
*.o
.pre-commit-config.yaml

# Clash compiler debug output
.clash/

# Dogfooding checkouts. This branch (dogfood/deps) vendors ALL of them:
# bittide-hardware plus the instrumented clash-protocols, clash-protocols-memmap,
# clash-cores and circuit-notation it builds against.

# Generated waveforms: regenerate with the commands in deps/README.md rather
# than committing megabytes of VCD.
waveforms/

# Local-only nix/devenv shim that leaks into the checkout.
/deps/bittide-hardware/devenv.sh
/adt-sidecar.vcd
/adt-sidecar.json
